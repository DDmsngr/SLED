/// Два режима работы карты.
enum MapMode {
  /// Разведка: трек не пишется, только текущая позиция + все POI.
  /// Кнопка «Пынь» (добавить POI) активна.
  scout,

  /// Запись следа: рисуется текущая Polyline.
  /// Все сохранённые POI рендерятся поверх для навигации в лесу.
  tracking,
}
