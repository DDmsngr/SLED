# IDE — launch.json содержит API ключи, не коммитить
.vscode/launch.json

# Flutter/Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
*.iml
*.class

# Android
android/local.properties
android/.gradle/
android/app/debug/
android/app/profile/
android/app/release/
android/key.properties
*.keystore
*.jks
# Разрешаем ТОЛЬКО debug.keystore — фиксированная подпись для debug/CI сборок,
# чтобы APK обновлялся поверх без удаления. Никогда не коммитить release-keystore.
!android/debug.keystore

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Generated files (hive adapters pre-included, rebuild with build_runner)
# lib/**/*.g.dart  # kept in repo for convenience
